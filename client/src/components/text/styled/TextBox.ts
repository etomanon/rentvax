import styled from 'styled-components'
import { space, SpaceProps } from 'styled-system'

export const TextBoxWrapper = styled.div<SpaceProps>`
  border-radius: 4px;
  box-shadow: 0px 5px 1rem 0px rgba(0, 0, 0, 0.25);
  ${space}
`
